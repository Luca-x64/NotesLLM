services:
  app:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    volumes:
      - database:/data
    depends_on:
      - ollama
  ollama:
    image: ollama/ollama:latest
    volumes: 
      - ollama_data:/root/.ollama
      
volumes:
  database:
  ollama_data: